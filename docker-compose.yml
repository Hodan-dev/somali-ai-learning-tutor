services:
  mongo:
    image: mongo:7
    container_name: somali-tutor-mongo
    ports:
      - '27017:27017'
    volumes:
      - somali_tutor_mongo:/data/db
    restart: unless-stopped

volumes:
  somali_tutor_mongo:
